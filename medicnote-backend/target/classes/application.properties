# ===== Server =====
server.port=8080

# ===== MySQL Database =====
# Create the database first:  CREATE DATABASE medicnote_db;
spring.datasource.url=jdbc:mysql://localhost:3306/medicnote_db?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Charitha@17
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== JPA / Hibernate =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ===== JWT =====
# 256-bit secret used to sign tokens - change this in production
jwt.secret=ZaalimaMedicNoteSuperSecretKeyForJWTSigning2026ChangeMeInProd
jwt.expiration.ms=86400000

# ===== File upload (not strictly required, kept for future use) =====
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
